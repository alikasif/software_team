from setuptools import setup, find_packages

setup(
    name='custom_log_framework',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
    ],
)
